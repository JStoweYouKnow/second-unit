/** Short-lived signed URLs for confidential assets (5 minutes). */
export const SENSITIVE_SIGNED_URL_TTL = 300

/** Standard signed URL TTL for non-sensitive files (1 hour). */
export const STANDARD_SIGNED_URL_TTL = 3600
